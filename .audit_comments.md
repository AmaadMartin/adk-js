core/src/code_executors/unsafe_local_code_executor.ts:L210: shrink redundant resolved flag and helper function. Call resolve directly in child error and close handlers.
core/src/agents/instructions.ts:L45: delete unreachable check and unused parameter. Remove !isValidStateName(key) check and rawMatch parameter from resolveKey.

net: -15 lines possible.
