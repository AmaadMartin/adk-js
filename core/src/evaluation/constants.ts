/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */

/**
 * Timeout in seconds for waiting for a model turn to complete in live mode.
 */
export const DEFAULT_LIVE_TIMEOUT_SECONDS = 300;

/**
 * Number of inferences an eval runs at the same time by default.
 */
export const DEFAULT_INFERENCE_PARALLELISM = 4;

/**
 * Number of metric evaluations an eval runs at the same time by default.
 */
export const DEFAULT_EVALUATE_PARALLELISM = 4;
