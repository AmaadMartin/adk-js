dist/
node_modules/
dev/src/browser

# Generated by `npm run generate:agent-config-schema`. The pre-commit hook runs
# prettier over JSON, which would rewrite the file and fail its own drift test.
core/src/agents/configs/AgentConfig.json
