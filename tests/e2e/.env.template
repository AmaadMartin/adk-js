GEMINI_API_KEY="your-api-key-here"

# GOOGLE_CLOUD_PROJECT gates the Vertex AI suites (e.g. live_model_test.ts).
# GOOGLE_CLOUD_LOCATION is optional for those suites and defaults to
# us-central1 when unset. The context compaction suite requires it, so keep it
# set here.
GOOGLE_CLOUD_PROJECT="your-project-id"
GOOGLE_CLOUD_LOCATION="us-central1"
