# Copy this file to tests/e2e/.env (git-ignored).
# That is the only .env the e2e suite reads; a value already exported in your
# shell takes precedence over the file.
GEMINI_API_KEY="your-api-key-here"
