# OpenAPIToolset Sample (`v0.1.0` Parity)

Demonstrates creating an `OpenAPIToolset` from an OpenAPI 3.0 specification with `tokenToSchemeCredential` and attaching it to an `LlmAgent`.

## Files
- `agent.ts`: Configures `OpenAPIToolset` and `rootAgent`.
